{
  "name": "@dudykr/trustable-workspace",
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": "20"
  },
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "dev": "turbo dev"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.5.12",
    "turbo": "^2.1.1",
    "zx": "^7.2.3"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "**/*": "prettier --write --ignore-unknown"
  }
}
